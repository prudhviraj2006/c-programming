#include<stdio.h>
int main()
{
  int a[]={10,20,30,40,50};
  int size=5,i,max,min;
  min=a[0];
  max=a[0];
  for(i=0;i<5;i++)
  {
    if(a[i]>max)
    {
      max=a[i];
    }
    if(a[i]<min)
    {
      min=a[i];
    }
  }
  printf("%d\n%d\n",max,min);
  return 0;
}
