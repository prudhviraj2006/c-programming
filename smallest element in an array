#include<stdio.h>
int main()
{
  int a[]={5,2,8,7,3};
  int size=5,i,min;
  min=a[0];
  for(i=0;i<5;i++)
  {
    if(a[i]<min)
    {
      min=a[i];
    }
  }
  printf(" the minimum number in an array is : %d",min);
  return 0;
}  
